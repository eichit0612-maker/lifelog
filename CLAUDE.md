@AGENTS.md

# このリポジトリでの進め方

## ページを変更したら、確認を取らずに公開まで済ませる

利用者から常時の許可を得ている（2026-09-10）。表示・データ・スタイルを変更したら、
毎回確認を取らずに次をまとめて実行する。

1. `CHANGELOG.md` の先頭に、その日の見出しで変更内容を追記する
   （何をどう変えたか。理由が自明でないものは理由も。日付は実際の日付）
2. `npm run db:checkpoint` — DB を触ったなら必ず。WAL に残った記録が公開に反映されないため
3. `npm run lint` と `npm run build:pages` が通ることを確認する
4. commit して `git push`
5. GitHub Actions の完了を待ち、公開ページが更新されたことまで確認して報告する

変更ログを残すことが条件になっている。省略しない。

## 確認を取るべきもの

上の常時許可は「ページの変更」に限る。次は必ず事前に確認する。

- 記録データの削除（`npm run db:reset` を含む。既存の記録が消える）
- リポジトリの公開範囲の変更、リポジトリ自体の削除
- 公開範囲に影響する変更（載せる項目を増やす、住所や日程の粒度を細かくするなど）

## 公開範囲についての前提

GitHub Pages のサイトは URL を知っていれば誰でも閲覧できる。
実在の店・チーム・日程を載せるページなので、次を守る。

- 実在チームの試合結果を推測で埋めない。分からないものは「予定」のままにする
- 評価とひとこと感想は本人が書くもの。こちらで創作して埋めない
- 自宅が特定できる粒度の住所や、留守が分かる旅行予定を足すときは確認する

## 開発メモ

- `npm run dev` は通常の Next.js サーバー。Server Actions が使えるのでここが記録の入力口になる
- `npm run build:pages` は `STATIC_EXPORT=1` で静的化する。公開サイトでは Server Actions は動かない
- 和文フォントは Google Fonts から読まない（数MBになる）。システムの明朝/ゴシックを使う

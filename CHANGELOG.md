# 変更ログ

公開サイト（https://eichit0612-maker.github.io/lifelog/）に反映された変更を新しい順に記録する。
main に push すると GitHub Actions が自動でビルドし直して公開される。

## 2026-09-12

### バスケに滋賀レイクスの全日程を入れた
- 2026-27 レギュラーシーズン全60試合（2026-09-25 〜 2027-05-02）を登録した。
  公式サイト https://shigalakes.com/schedule/list/ から取得したもので、推測は入れていない
- 対戦相手は公式表記の略称のまま（京都 / 琉球 / A東京 など）。ファンの表記に合わせた
- 1月以降は開始時刻が未発表のため「時刻未定」と表示する。会場の「(予定)」表記も公式のまま
- 結果は入れていない。各行に点線の空欄を置き、観たあとに書き込む場所として残した
- リーグ表記を `B.LEAGUE` から `B.PREMIER` に変更（2026-27 から最上位リーグの名称が変わったため）

### 仮で入れていた NBA の3試合を削除した
- 実在チームの架空の対戦カードだったため。実際の日程が要るなら改めて取得する

### 表示
- 試合一覧をチーム＋リーグでまとめ、その中を月で区切るようにした。60試合を一列に並べると追えないため
- 各行に曜日とホーム/アウェイ（H は朱色、A は控えめ）を表示するようにした
- チーム名は見出しに出し、行は対戦相手だけにした。同じ名前が60行続くのを避けるため

### スキーマ
- `basketball_games` に `is_home` を追加（0/1）。会場名から判定するのは壊れやすいため

## 2026-09-10

### 見た目を全面的に作り直した
- 配色をグレー系から紙色（`#faf8f4`）と墨色に変更。差し色は朱色ひとつだけにした
- タブの絵文字を削除。文字と朱色の下線のみにした
- 影付きカードをやめ、罫線で行を区切る台帳形式にした
- 統計カード4枚を、罫線に挟んだ一行の数字に置き換えた
- 評価を★5つから小さな升目5つに変更。未評価は空欄のまま置く
- 見出しを明朝、数字と欧文を EB Garamond に変更。和文はシステムフォントを使う
  （Google Fonts の日本語は数MBになり静的サイトには重いため）
- 空のタブに破線の箱を置くのをやめ、一行の断り書きにした

### 記録の中身を入れ替えた
- グルメ: 都路里 / 塩元帥 / きときと寿司 の3件
- 旅行: 中身なし（タブのみ）
- バスケ: 滋賀レイクス 3試合、NBA 3試合。すべて予定として登録
- 鑑賞: ちいかわ / メダリスト / ハイキュー!! / エヴァンゲリオン（リバイバル上映） / ヤニねこ の5件
- 評価と日付は未入力にしてある。実際に行った・観たときに埋める前提

### スキーマ
- `basketball_games` に `team`（応援チーム）と `league` を追加。
  自分が出る試合ではなく観戦記録として扱うため。勝敗は `team` から見た結果になる
- `watch_logs.watched_on` を任意に変更。タイトルだけ先に登録できるようにした
- 日付が未入力の行は一覧の末尾に、登録した順で並ぶようにした

### CI
- `package-lock.json` を npm 10 で生成し直した。npm 11 が生成した lock は
  `@emnapi/*` を含まず、CI の `npm ci` が EUSAGE で失敗していた
- ワークフローの Node を 24 に固定し、`checkout` と `setup-node` を v5 に更新
- `npm ci` 失敗時に npm の出力を注釈として出す診断を追加
  （Actions のログ本文はリポジトリ管理者としてサインインしないと読めないため）
- SQLite の DB が改行変換で壊れないよう `.gitattributes` で binary 指定

### 公開
- GitHub Pages で公開を開始した

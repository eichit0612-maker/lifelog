# better-sqlite3 は binding.gyp を同梱しているため、npm が自動で `node-gyp rebuild` を
# 実行しようとする。だが win32-x64 / linux-x64 などのビルド済みバイナリが
# node_modules/better-sqlite3/prebuilds/ に同梱されており、自前ビルドは不要。
# Visual Studio が無い環境でも入るようにインストールスクリプトを止める。
#
# 注意: postinstall が必須の依存を今後追加した場合は、この設定を見直すこと。
ignore-scripts=true
